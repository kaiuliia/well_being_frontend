import { createSlice } from "@reduxjs/toolkit";
const surveySlice = createSlice({
  name: "survey",
  initialState: {
    dashBoardData: [],
  },
  reducers: {
    // addSurvey(state, action) {
    //   state.dashBoardData.push({
    //     // newSurvey,
    //   });
  },
  // getWholeDashboard(state, action) {},
  // },
});
