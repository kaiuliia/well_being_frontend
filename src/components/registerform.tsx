import React from 'react';

interface Props {
    title: string;
}


    export function  Register(props: Props) {
        return  (
            <div>{props.title}</div>
        );
    };

    // export default Register;

